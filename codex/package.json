{
  "name": "bugcat-house",
  "description": "Bugcat House",
  "scripts": {
    "test": "hardhat test",
    "compile": "hardhat compile",
    "deploy:holesky": "hardhat run scripts/deploy-holesky.js --network holesky",
    "verify:holesky": "hardhat run scripts/verify-holesky.js --network holesky"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.4.0",
    "dotenv": "^17.2.1",
    "hardhat": "^2.26.3",
    "@ensdomains/ens-contracts": "^1.5.2",
    "solady": "^0.1.26"
  }
}
